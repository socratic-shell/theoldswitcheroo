{
  "name": "theoldswitcheroo-daemon",
  "version": "1.0.0",
  "description": "Portal communication daemon and CLI tools",
  "main": "daemon.js",
  "scripts": {
    "build": "node build.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "commander": "^11.0.0"
  }
}
